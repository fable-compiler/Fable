module Lib

module Bar =
    let bar = "hoho"

module Bar2 =
    let bar = "hihi"

// type Foo =
//     { x: int }
//     member __.Z() = 5
//     member this.Add(x, y) = x + y + this.Z()
//     static member Add(x, y) = x - y