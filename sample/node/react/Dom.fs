namespace Fable.Import
open System

[<AutoOpen>]
module Dom =
    type DataTransfer =
        interface end

    and Event =
        interface end

    and EventTarget =
        interface end

    and Document =
        interface end

    and HTMLElement =
        interface end

    and Element =
        interface end

    and StyleMedia =
        interface end

    and SVGElement =
        interface end
